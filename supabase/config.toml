project_id = "<your-project-ref>"
# For local dev, Supabase CLI fills connection details.
# For remote operations, set `project_id` to your Supabase project ref (e.g., abcdefghijklmnox).

[db]
# Optional: configure shadow DB or overrides for migrations if needed

[studio]
port = 54323

[api]
port = 54321

